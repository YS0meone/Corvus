# Docker Compose service defaults
# docker-compose.yml uses hardcoded defaults — edit it directly to change them.
#
# Postgres (port 5432)  user=postgres  password=postgres  db=langgraph
# Redis   (port 6379)   no auth
# Qdrant  (port 6333)   no auth
# Grobid  (port 8070)   no auth
